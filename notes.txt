not a good idea for the llm to ask what manual is this referrign to. instead its better to make this a separate system.
i would advise for llm to give its understanding of the manual parts, but at the same time to provide the excerpts and
page in the manual where it found the issue
-why these embeddings. discuss in your notes every single line, why thta decisionwas made exactly and what are the potential challenges 
how i set up the environment step by step
-what is a namespace and why didnt u use it
-why cloud aws and that region (cus only free one)

-searching a query vector https://docs.pinecone.io/reference/api/2025-01/data-plane/query vs this type of search https://docs.pinecone.io/guides/data/query-data
-installed poppler cus catterpillar counldnt load

-assured that the total vector count is equal to  number of pages
-why did i choose pages ass chunks, problemsatics with that, 
- can a vector be reverted back to the original text, why didnt i add the value of the vector together with the vector to pinecone db (answer: cost)

-Hlustration  data cleanup: ocr extrsacted pages had some funky working and characters.
-order of results (first displaying page 125, then page 123...)

-how did i decide on top k ? - i saw in your script what pagesshould be retrieved, i saw a total of 7 pages, 
added an extra 1 for comfort. irl this is how i woud do it:

-how did i choose metric and namespaces
-inefficiencies : one fucntion, the one on extracting text from pdf could be modular, this way we are rewriting two similar funcitons through two scripts

-what is an index

-document summary index as a separate index? or add docuemnt summary as a chunk https://docs.llamaindex.ai/en/stable/examples/index_structs/doc_summary/DocSummary/

-alternatives: llamaindex can perfrm rag, 
-like this we are running vector seatch twice but thats not a problem cus its not expensive. 
 like  tihs we are still in line with the requirements of the assignment, though i would suggest a different system
 where we are asked to sleect a manual in the beginning.. the process of looping through multiple manuals and double vector
 search could be made simpler. why again vector search? because we need to retrieve the appropriate amount of chunks 
 -why cloud ufnciton and not cloud run?

 -pubsub event vs message?

 -The assumption was that we don't have to ingest  many PDFs at the same time. If we did need bulk processing, each uploaded pdf would send a pubsub message, and have multiple Cloud Run Workers subscribe and process PDFs in parallel. 
 -We know that OCR is needed if we obtain empty chunks at the end of text extraction from a document. Potential challenge with this approach: What if the document is partially scanned? We need to  introduce logic that understands if the document has any scanned data in it. so that we don't lose the scanned data.
 - Upon further research, turns out there are python packages like Fitz that extract text both from searchable and non-searchable documents. 
 -if processing fails at any point, messages can be retrieved using Dead Letter Queue
 - Chunking per page is the simplest - however, chunks with overlap might be a good choice too. They assure the sentences don't get split between pages. This results in higher recall: more overlap means less risk of missing relevant information.
 -Firestore here acts as a buffer in case embeddign fails, enables parallel processing, and is good to have visibility over text chunks.
 -for a robust system, I would advise also storing embeddings in firestore, in case we lose data from pinecone. It might also help with debugging Pinecone.
 -Error logging: If text extraction or Embedding fails, log error to GCP's Error Reporting API. Retry if failure count<n, else store document in Error queue
 -selection of a manual could be automated further: if several manuals are retrieved, but it's very clear from the confidence scores which manual the user wanted, run vector search again to assure you retrieved all relevant chunks from that manual.